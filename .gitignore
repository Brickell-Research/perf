# Benchmark results (regenerated via `make bench`)
results/

# bench-local.sh output (regenerated on every run)
results-local/

# Downloaded binaries (managed by cvm now)
bin/

# Python
__pycache__/
*.pyc

# macOS
.DS_Store

# Erlang crash dumps
erl_crash.dump
